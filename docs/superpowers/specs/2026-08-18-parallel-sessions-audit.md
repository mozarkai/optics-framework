# Parallel sessions & async-correctness audit — optics-framework

Consolidated from five independent audits at `2a233a0`. Deduplicated; each finding has a stable ID for reference in the implementation plan.

**One-line answer to "why doesn't it support multiple sessions in parallel?"**
Not one reason. Four independent families, any one of which alone would break it:
1. The keyword execution path blocks the event loop (`A1`), so concurrency is cooperative in name only.
2. Five process-global singletons hold what should be per-session state (`D1`–`D5`).
3. Every artifact path is per-*project*, not per-*session*, and two of them are non-atomic read-modify-writes (`E1`–`E3`).
4. Nothing allocates the exclusive external resources parallel sessions need — most starkly, `systemPort`/`wdaLocalPort` are **never set anywhere in the tree** (`F1`).

**The good news, stated up front.** The correct architecture already exists in exactly one place — `optics_framework/common/execution.py:189`:
```python
async with session.keyword_lock:
    result = await asyncio.to_thread(method, *deserialized_params)
```
Sync engines, one async seam, per-session lock. The work is not *adding* concurrency; it is making that the only seam and deleting the state that assumes one session. And `Appium.attach_to_session` (`optics_framework/engines/drivers/appium.py:391`) is a working re-attach to an existing remote session — currently unreachable from HTTP, and the single highest-leverage asset for statelessness.

---

## A. Blocking the event loop

| ID | Finding | Location | Sev |
|----|---------|----------|-----|
| A1 | **The entire `optics execute` keyword path calls keywords directly on the loop** — bare `method(*args, **kwargs)`, no `to_thread`. Chain: `asyncio.run` (`helper/execute.py:678`) → `BaseRunner.run` → `ExecutionEngine.execute` → `BatchExecutor.execute` → `await runner.run_all()` → `_process_test_case` → `_process_module` → `async def _execute_keyword` → `async def _try_execute_with_fallback` → **`method(...)`**. Loop dead for 200ms (tap) to 30s (`Assert Presence`). `EventManager._process_events` can't run, so the Rich tree and JUnit XML only advance *between* keywords. Under serve, freezes the whole server incl. `/health`. | `common/runner/test_runnner.py:436` | BLOCKER |
| A2 | `delete_session` runs driver teardown on the loop: `driver.quit()` → `event_sdk.send_all_events()` → 3-attempt retry with `time.sleep(retry_delay * attempt)` (0+2+4s) around `requests.post(timeout=10)`. **Worst case ≈36s with the loop fully stopped**, then `shutil.rmtree` also on the loop. Reached from MCP too. | `common/expose_api.py:1341` → `session_manager.py:176` → `engines/drivers/appium.py:519,529,531` → `common/eventSDK.py:298,159` | BLOCKER |
| A3 | **All 15 FastAPI endpoints are `async def`; zero `def` handlers.** anyio's 40-thread pool is never used, so any blocking call that slips in freezes the entire server instead of occupying one worker. This is the structural reason A2 is severe. | `common/expose_api.py:397,457,880,956,988,1023,1031,1039,1066,1074,1087,1099,1121,1323` | HIGH |
| A4 | `pytest.main([...])` — the whole suite, minutes — runs on the loop, plus `open().write()` and `shutil.rmtree` around it. | `common/runner/test_runnner.py:1193`→`1253` | HIGH |
| A5 | **Busy-spin loops with no throttle at all.** `while time.time() - start < timeout:` calling `get_page_source()` (full Appium XML round-trip) with no sleep — and the throttle is present as a *commented-out* `# Optional: time.sleep(0.3)  # Delay to reduce busy looping`. A 30s assertion becomes thousands of Appium requests with the loop dead. | `engines/elementsources/appium_page_source.py:211-229`; `appium_find_element.py:203` | HIGH |
| A6 | `time.sleep` on the keyword path, ~25 sites. Notably the **public `Sleep` keyword is a bare `time.sleep`** (whole function body). | `api/action_keyword.py:1067,736,821,888`; `common/strategies.py:305,308,416,419`; `common/ai_self_heal.py:380,389`; `common/utils.py:273`; `elementsources/selenium_find_element.py:172`, `selenium_page_source.py:194`, `playwright_find_element.py:251`, `playwright_page_source.py:1037`; `engines/drivers/ble.py:224,294,361,385,464,558` | MED |
| A7 | Synchronous network clients on the keyword path: `requests.request` (the `Make API Request` keyword) + HAR/log writes; remote OCR/OIR `requests.post`; blocking gRPC to Google Cloud Vision; `eventSDK` `requests.post(timeout=10)`. `eventSDK` also **re-reads its attributes JSON from disk per event**. | `api/flow_control.py:1082,1109,1184,1197`; `vision_models/ocr_models/remote_ocr.py:58`; `image_models/remote_oir.py:53`; `ocr_models/googlevision.py:96`; `common/eventSDK.py:159,70-71` | MED |
| A8 | `execute_keyword` rebuilds the **entire keyword registry per HTTP request, on the loop** — `session.optics.build(ActionKeyword/AppManagement/Verifier)`, each constructing a `StrategyManager` whose `_is_method_implemented` calls **`inspect.getsource(method)`** and string-parses the body, per strategy × per element-source × per request. If `easyocr` is configured, first instantiation is a multi-second model load that may download weights. Worse: the `method` whose signature drives param binding is **not the object actually invoked** — `RunnerFactory.create_runner` builds a second independent set. | `common/expose_api.py:905-912,915`; `common/strategies.py:78-93`; `common/execution.py:245-252` | MED |
| A9 | **Two `threading.Lock`s are acquired from the loop thread while a worker thread can hold them across blocking disk I/O.** JUnit `flush()` does `mkdir` + `ET.tostring` + `minidom.parseString` + `toprettyxml` + `write`, re-serializing the whole accumulated tree, called from `close()` **while holding `self._lock`**. `setup_junit_for_session` runs in a `to_thread` worker; `cleanup_session` runs on the loop. Full server stall on a plain mutex. (Nothing is held across an `await` — it's the inverse problem.) | `common/Junit_eventhandler.py:19,272-285,288,26,62`; `common/events.py:179` | MED |
| A10 | `create_session` correctly offloads `session_manager.create_session`, but `discover_templates` (full directory walk) runs on the loop *before* the offload and `reconfigure_logging` (`mkdir` + handler construction) runs on the loop *after* it. | `common/expose_api.py:502,522` | MED |
| A11 | `_capture_end_of_run_artifacts` is sync, called from `async def run_all`: builds a fresh `StrategyManager`, two driver round-trips, `cv2.imwrite`, three file writes, `json.dump`. | `common/runner/test_runnner.py:815,492-552` | MED |
| A12 | `optics live` TUI: three plain-`def` handlers block the prompt_toolkit loop — `/save` (`makedirs`×3, 3 CSV appends, `rmtree`+`copytree` of the whole artifacts dir), `list_devices` (`subprocess.run(["adb","devices"], timeout=10)` + `idevice_id -l` = **up to 20s of frozen UI**), and `_switch_device` (rebuilds the Appium session synchronously — note `live_tui.py:843` *does* wrap `switch_device` in an executor, so the two paths are inconsistent). | `helper/live_tui.py:701,736,753`; `helper/live.py:785-823,988,1013,1042` | MED |
| A13 | `camera_screenshot` blocking socket reads / `cap.read()` with no offload. | `engines/elementsources/camera_screenshot.py:138,217-218,235-240` | LOW |
| A14 | `asyncio.Queue()` / `asyncio.Lock()` constructed in a `to_thread` worker. Safe on 3.12 only because `_LoopBoundMixin` binds lazily — **do not "optimize" these constructors into an eager-loop form.** | `common/session_manager.py:145-146`; `common/events.py:74` | LOW |
| A15 | Deprecated `asyncio.get_event_loop()`. | `common/events.py:85`; `common/execution.py:374`; `common/expose_api.py:1215,1236,1239`; `helper/live_tui.py:563,632,786` | LOW |

**Does the `cfda461` fix generalize? No.** It wrapped exactly two things — `KeywordExecutor.execute`'s method call and `session_manager.create_session`. It does not cover `BatchExecutor`/`DryRunExecutor` (A1 — the pattern needed to go one layer deeper, into `TestRunner`), `delete_session` (A2 — the mirror image, left unwrapped), `PytestRunner` (A4), or the per-request registry rebuild (A8, which sits *outside* both the lock and the thread). Note also `TestRunner._try_execute_with_fallback` has **no `keyword_lock` equivalent** — fixing A1 by dropping `to_thread` in *without* a per-session lock would let concurrent test cases race the same driver.

## B. Sync/async bridge bugs

| ID | Finding | Location | Sev |
|----|---------|----------|-----|
| B1 | **`queue_event_sync` calls `asyncio.run()` from inside a running loop, which always raises, and `except RuntimeError` swallows it.** Verified empirically. In `--runner pytest` mode **100% of events are dropped** — no JUnit XML, no tree output, silently. All 8 call sites affected. | `common/runner/test_runnner.py:88-95`, call sites `:901,914,929,942,962,1014,1266,1280` | HIGH |
| B2 | **One process-global event loop thread serves every Playwright driver**, with a hard-coded `future.result(timeout=15)`. ~35 call sites. `navigation_timeout_ms` defaults to 60000 and therefore **can never be honoured** — guaranteed spurious `E0102`. `_executor = ThreadPoolExecutor(max_workers=1)` is dead code. `_get_or_create_persistent_loop` recreates the loop if closed, orphaning any Playwright object bound to the old one. | `common/async_utils.py:8-13,25,56-64`; `engines/drivers/playwright.py:110` | HIGH |
| B3 | **The per-session `EventManager` is `start()`ed and `shutdown()`n on every keyword request.** `keyword_lock` covers only the two lines at `execution.py:189-190`, not the surrounding engine body. Two concurrent calls on one session: A's `finally` cancels the shared `_process_task`; B then publishes into a queue with `_running == False` and no consumer → **events silently lost**, and B burns the full `OPTICS_EVENT_DRAIN_TIMEOUT_S` (2s) waiting for a queue nobody reads. Plus `asyncio.sleep(0.1)` drain latency per request even in the happy path. | `common/execution.py:396,428`; `common/events.py:92-97,159` | HIGH |

## C. Logging

**Verdict: yes, logging is a real concurrency problem — but the handlers being sync is the least of it.** The async-safe pattern is half-built and dead; the actually-used path renders Rich on the event loop; and log capture mutates a global logger per keyword.

| ID | Finding | Location | Sev |
|----|---------|----------|-----|
| C1 | **`QueueHandler` is attached but `QueueListener` appears ZERO times in the repo.** `execution_log_queue = queue.Queue(-1)` is unbounded and never drained — every execution `LogRecord` (with `args`, `exc_info`, captured frames) retained for the process lifetime; `clear_queues()` only runs at `atexit`. In a long-lived server that is a steady unbounded leak. Separately, `execution_console_handler` is constructed and formatted but **never attached to any logger** — execution logs reach a console only via `propagate = True` → root. | `common/logging_config.py:94,103-110,265`; `stop_listeners` `:48` | BLOCKER (mem) |
| C2 | **Every session reconfigures global logging.** `ConfigHandler.__init__` calls `initialize_handlers(config)` — once per `Session`. It sets levels on the two module-global loggers and adds a `RotatingFileHandler` deduped **only by `baseFilename`, never removed**. Session B (`DEBUG`) silently flips session A (`INFO`) mid-run, and once B's file handler is attached, **A's log lines are written into B's log file too** — every session's log becomes a superset of all concurrent sessions. | `common/config_handler.py:140`; `common/logging_config.py:24-46`; `common/expose_api.py:522`; `helper/live.py:383` | BLOCKER (isolation) |
| C3 | `optics serve` **replaces `uvicorn.*` and root logger `handlers` wholesale and forces root to DEBUG**, and `expose_api.py:498` hard-codes `log_level=DEBUG` into every session `Config`. Every record from `urllib3`, `selenium`, `asyncio`, `appium`, plus every HTTP access line, is Rich-rendered — markup parse, syntax highlight, terminal measure, write+flush — **on the event loop thread**. | `helper/serve.py:33-49`; `common/expose_api.py:498,522` | HIGH |
| C4 | Eager f-string formatting on the hot event path: `internal_logger.debug(f"Processing event: {event.model_dump()}")` — a full Pydantic serialization executed **before** the level check, for every event × every subscriber. | `common/events.py:105-109`; `common/Junit_eventhandler.py:126-127` | HIGH |
| C5 | **`LogCaptureBuffer` is added to the process-global `execution_logger` per keyword** and removed after. With two sessions, A's per-keyword buffer captures **B's records**, which land in A's JUnit `<log>` elements. `emit` is serialized by `Handler.lock`, but `get_records()`/`clear()` are unsynchronized and `_send_event` reads `.records` with no lock. | `common/runner/test_runnner.py:326-327,370,420,446,473,488`; `common/Junit_eventhandler.py:218-223` | HIGH |
| C6 | `SensitiveDataFormatter.format` **mutates the shared `LogRecord`** (`record.msg = self._sanitize(...)`). A record is shared across all handlers and the propagation chain; racy once written from multiple threads. | `common/logging_config.py:114-119` | MED |
| C7 | `disable_logger()` sets `logging.getLogger().disabled = True` — **one session's shutdown kills logging process-wide.** Only reached from `atexit` today. | `common/logging_config.py:81,216,274` | MED |
| C8 | `LogCaptureBuffer` is **duplicated verbatim** in two modules; `test_runnner.py` imports the `logging_config` copy, not the `Junit_eventhandler` one. | `common/logging_config.py:140`; `common/Junit_eventhandler.py:93` | LOW |
| C9 | `SensitiveDataFormatter` is applied only to **file** handlers (`use_sensitive=True`), not the console handler — so redaction does not cover stdout, which is exactly where a containerized deployment ships logs. | `common/logging_config.py:184-186` | MED (see I4) |

**Not a problem:** `logging.basicConfig` is called exactly once, in a codegen function (`helper/generate.py:781`) — not at import time, not in any async path.

## D. Process-global singletons holding per-session state

| ID | Finding | Location | Sev |
|----|---------|----------|-----|
| D1 | **`TreeResultPrinter` is a process-wide singleton whose `test_state` is wholesale-replaced per session.** B's `TestRunner.__init__` assigns a fresh dict → A's rows vanish → A's `_update_status` does `test_state.get(name)` → `None` → **silently no-ops**, so A's live tree freezes with no error. Whichever finishes first calls `stop_live()`, killing the other's display. `start_run` overwrites `task_id`, so the first session's progress bar never completes. Verdict consumers compute `all(tc.status == "PASS" ...)` over the **union** of both sessions' test cases. `DryRunExecutor.execute` *returns* this shared dict. `execution.py:425-427` logs "Stopping result printer live display" but **never calls `stop_live()`**. Mitigated for serve only: `NullResultPrinter` is per-instance, so `use_printer=False` dodges it. | `common/runner/printers.py:131,148-150,153,170,224,236,243`; `common/execution.py:255,70,118,425-427`; `common/runner/test_runnner.py:178,255,814` | BLOCKER |
| D2 | **`PytestRunner.instance` is a class attribute wired into a generated `conftest.py` fixture.** B's `__init__` overwrites it → A's generated test file executes **B's** keyword map, elements, and driver. Both write the same `junit_output.xml`, both mutate `sys.modules` and pytest's global plugin/config state concurrently. | `common/runner/test_runnner.py:839,860,1227-1235,1246-1252` | BLOCKER |
| D3 | **One `GenericFactory._registry` is shared by all five factory subclasses** — none of `DeviceFactory`/`ElementSourceFactory`/`ImageFactory`/`TextFactory`/`LLMFactory` overrides it. `create_instance_dynamic` unconditionally caches `instances[name] = instance`, and `clear_instances` is never called anywhere — so **every terminated session's driver, browser, and `easyocr.Reader` stays referenced forever**. Live paths always construct fresh, so today it's a leak not a swap; but `_create_or_retrieve` *returns* the cached instance and is reachable via `create_instance`, so the instant anything routes through it, session B silently receives **session A's driver, pointing at A's device**. `module_paths` is also a flat package-agnostic namespace, so two engine modules sharing a filename would alias. | `common/base_factory.py:23,108,115,129-131,155,201`; `common/factories.py:11,31,82,91,100` | BLOCKER (latent) |
| D4 | **`Optics` SDK facade is `@library(scope="GLOBAL")` with a single session slot.** Robot instantiates it once per process. Suite B's `Setup` overwrites `self.session_id`/`action_keyword`/`verifier`/`flow_control`; **session 1 is never terminated** (driver, temp dir, JUnit handler all leak) and every subsequent keyword from suite A drives session 2's device. | `optics.py:146,160,367` | BLOCKER |
| D5 | `console.force_terminal = True` **permanently mutates the process-global Console.** Set once, it survives the session and forces ANSI escapes into every later consumer of `get_console()`, including non-TTY captured output in server logs. Assessment of `e1763e7`: sharing one `Console` is *correct* — a second `Console` has its own cursor model and interleaving two is exactly what produces torn output. But rich raises `LiveError: Only one live display may be active at once`, and the only thing preventing that today is D1's singleton `if not self._live` guard — **the crash is masked by a worse bug.** | `common/runner/printers.py:237-238`; `common/error.py:432` | MED |

## E. Filesystem / artifact collisions

| ID | Finding | Location | Sev |
|----|---------|----------|-----|
| E1 | **`execution_output_path` is per-*project*, not per-*session*** — `<project_path>/execution_output`, or **`os.getcwd()/execution_output`** when `project_path` is `None`. Every writer below resolves that one directory. The root fix; most items below fall out of it. | `common/config_handler.py:126-135` | BLOCKER |
| E2 | **`page_sources_log.xml` is a non-atomic read-modify-write executed on nearly every keyword.** `open('r+')`, read whole file, `seek(0)`, strip trailing `</logs>`, rewrite entire file — no lock, no truncate. A reads 4MB and starts rewriting; B reads the same 4MB, appends, writes; A's write lands on top → B's entry lost, and because `r+` doesn't truncate, a shorter write leaves **trailing bytes after `</logs>`** → the `endswith("</logs>")` guard fails on the next call and page-source logging **silently stops for both sessions**. Called from `get_page_source`, i.e. every OCR/text-strategy locate. **The most likely concrete corruption in the codebase.** | `common/utils.py:541,549-563`; callers `engines/drivers/appium_UI_helper.py:36`, `api/verifier.py:280` | BLOCKER |
| E3 | `api_details.har` — `json.load` whole file, append, `json.dump` back. One entry lost per collision; a partial write leaves invalid JSON, after which `except json.JSONDecodeError` **silently discards the entire accumulated history**. `api_details.log` is a shared append with no session tag. | `api/flow_control.py:1078-1081,1183-1198` | RACE |
| E4 | `interactable_elements.json` — fixed name, unconditional full overwrite. | `common/utils.py:612-615`; callers `api/verifier.py:345,360` | RACE |
| E5 | `junit_output.xml` in the pytest runner has **no session id**, unlike the event-handler path which correctly writes `junit_output_<session_id>.xml`. | `common/runner/test_runnner.py:1252` vs `common/Junit_eventhandler.py:56` | RACE |
| E6 | `optics live` stamps are **second-precision with no session id** — two sessions in the same second share `screenshots/session_<stamp>/` (created `exist_ok=True`, so **silently**) and both `FileHandler`s open `logs/optics_live_<stamp>.log`. Separately `/tmp/optics_live_stderr.log` is a **fixed path opened `"w"` (truncating)** then `dup2`'d onto fd 2 — B truncates the file A's fd 2 points at; also a predictable-path write in a world-writable dir. | `helper/live.py:310,315-318,383,1384,1393,1430` | RACE |
| E7 | `optics live` `/save`: TOCTOU between `_existing_names` check and append → two sessions both pass the conflict check, both append → duplicate module rows, resolved downstream by a "Duplicate modules key … Overwriting" warning, so **one recording is silently lost**. Then `shutil.rmtree(destination)` + `copytree` → A's `rmtree` **deletes the artifacts B is mid-`copytree` into**. | `helper/live.py:797-799,802-815,818-822`; `helper/execute.py:551-557` | RACE |
| E8 | **`save_captures=False` is honoured by `Verifier` only — `ActionKeyword` and both UI helpers write regardless.** `optics serve` sets `save_captures=False` precisely so HTTP sessions don't touch disk, but every keyword still writes a JPG plus an E2 `page_sources_log.xml` rewrite into the server's shared cwd. N concurrent HTTP sessions → unbounded shared-directory writes and the E2 corruption, **in the one deployment where parallel sessions are the norm.** | `api/verifier.py:26` vs `api/action_keyword.py:299,61,87,108,500,954,976,1014,1124`; `engines/drivers/appium_UI_helper.py:36`, `selenium_UI_helper.py:32`; `common/expose_api.py:499` | RACE |

## F. External resource exclusivity — nothing exists today

| ID | Finding | Location | Sev |
|----|---------|----------|-----|
| F1 | **`systemPort` / `wdaLocalPort` / `mjpegServerPort` / `chromedriverPort` — zero hits across the entire tree.** These are *mandatory-unique* per parallel Appium session (UiAutomator2 defaults to 8200, WDA to 8100). Two sessions with the same `config.yaml` → Appium rejects the second, or both UiAutomator2 servers race for 8200 and **commands land on the wrong device**. | absent; inject at `engines/drivers/appium.py:102` | BLOCKER |
| F2 | **No device lease.** No `fcntl`/`flock`/`filelock`/port-picker anywhere in the tree. No allocation table. Nothing stops two sessions grabbing the same `udid`. Both also call `adb` against the same serial and `ideviceinstaller -u <udid>` with no serialization. | `engines/drivers/appium.py:62-63,593-617,640-648,1326`; `helper/live.py:989,1042-1076` | BLOCKER |
| F3 | Shared exclusive endpoints with no arbitration: Appium `127.0.0.1:4723`, Selenium `localhost:4444/wd/hub`, remote OIR `127.0.0.1:8080`, and a single `camera_index` / TCP `ip:port` for the camera source. | `engines/drivers/appium.py:58`; `selenium.py:31,94`; `image_models/remote_oir.py:26`; `elementsources/camera_screenshot.py:11-13,179` | MED |
| F4 | Two configs carrying the same `existingSessionId` both attach to the **same** Appium session, and `_cleanup_existing_driver` calls `driver.quit()` — **killing it for the other session**. | `engines/drivers/appium.py:150,184-210` | MED |

## G. Lifecycle / ops gaps

| ID | Finding | Location | Sev |
|----|---------|----------|-----|
| G1 | **`delete_session` cannot clean up a broken session.** It runs `close_and_terminate_app` *first* and only reaches `terminate_session` if that succeeds. Device reboots / Appium restarts / browser crashes → `driver.quit()` raises → 500 → the session is **never removed** from `sessions`, `workspace_hashes`, or the event registry, and its temp dir is never `rmtree`d. Retrying gives the identical 500. **There is no other eviction path.** And `terminate_session` already calls `driver.terminate()` itself, so the pre-call is redundant *and* the sole reason cleanup gets skipped. | `common/expose_api.py:1323,1334,1341,1343`; `common/session_manager.py:175-176`; `common/base_factory.py:248-249` | CRITICAL |
| G2 | Mirror bug on create: the session is registered, *then* the app is auto-launched. If `launch_app` raises, `except Exception` returns 500 **without terminating the half-built session** — which may hold a half-open Appium session. Secondary: that `except` swallows the `HTTPException` from `execute_keyword`, flattening a precise 4xx into a generic 500. | `common/expose_api.py:513-521,529-534,542-544` | CRITICAL |
| G3 | **No TTL, no expiry, no reaper, no idle timeout, no cap.** Grep `ttl\|expire\|reap\|last_used\|max_sessions\|Semaphore` across `expose_api.py`/`serve.py`/`session_manager.py` → **nothing**. `Session` records no `created_at`/`last_accessed`. A client that opens a session and disconnects holds a device **indefinitely**. Combined with G1/G2 this is unbounded resource exhaustion, and an unauthenticated DoS given I1. `expose_api.py:464-470` merely *logs a warning* about a second session — a fossil of the single-session design. | absent | HIGH |
| G4 | **No `lifespan`, no `@app.on_event`, no `atexit`, no signal handler.** On SIGTERM/SIGINT or a container rollout, uvicorn exits and **every live driver session is orphaned** — Appium sessions linger until the server's own timeout, Playwright browser subprocesses may survive, every `mkdtemp` dir is left on disk. | absent in `common/expose_api.py`, `helper/serve.py` | HIGH |
| G5 | No request timeouts. Appium's client `CONNECTION_TIMEOUT = 300` — **5 minutes**. A keyword blocking on a dead device holds `keyword_lock` and a thread-pool worker for that long; every other request on the session queues behind it. | `engines/drivers/appium.py:59`; `helper/serve.py:96-104` | MED |
| G6 | Starlette cancels the handler coroutine on client disconnect, but **`asyncio.to_thread` is not cancellable** — the driver call runs to completion regardless. | `common/execution.py:190` | MED |
| G7 | `event_generator` loops forever on `session.event_queue` and **never checks whether the session still exists** — after `terminate_session` it keeps heartbeating against a dead `Session`, holding it alive via the closure. `workspace_generator` *does* check and break; the two are inconsistent. | `common/expose_api.py:1263-1314` vs `:1220` | MED |
| G8 | **`--workers N` is advertised and silently broken.** It's wired to uvicorn with the app as an import string, so each worker imports `expose_api` and gets its **own** module-global `SessionManager` and event-manager registry. Client creates on worker 1, next request round-robins to worker 3 → **404 on a session that demonstrably exists**, ~1-in-N success. Meanwhile worker 1 holds a live Appium session with no way to reach or terminate it. | `helper/cli.py:123-129`; `helper/serve.py:96-104`; `common/expose_api.py:38`; `common/events.py:204` | CRITICAL |

## H. Concurrency correctness within serve

| ID | Finding | Location | Sev |
|----|---------|----------|-----|
| H1 | **The workspace SSE stream bypasses `keyword_lock` entirely.** `_gather_workspace_data` builds its own `Verifier` and calls screenshot / element-collection / page-source directly via `to_thread`, never taking the lock. A UI streaming a session while anything posts a keyword **interleaves WebDriver commands on the same session** → spurious stale-element / `NoSuchElement` failures; with Selenium/Playwright, protocol-level corruption. Also a data race on `InstanceFallback.current_instance`. The screenshot may be mid-transition, so `_compute_workspace_hash` churns and the stream floods. | `common/expose_api.py:1143,1150-1186,1204-1261`; `common/base_factory.py:239` | HIGH |
| H2 | **`request_template_overrides` is request-scoped data stored on the `Session` object**, written *before* the lock and `.clear()`ed in a `finally` *outside* it — clearing **everything, including another in-flight request's entries** — plus `rmtree` of its own temp dir. B writes its override; A completes and clears; B then resolves nothing, falls through to project templates, and fails with a bogus element-not-found. Or A's `rmtree` removes a dir B is about to read. | `common/session_manager.py:119`; `common/expose_api.py:854,896,938-946` | HIGH |
| H3 | `session.apis = api_data` with no synchronization — a concurrent in-flight API keyword reads a swapped-out collection mid-execution. | `common/expose_api.py:1004` | MED |
| H4 | `SessionManager.sessions` has **no lock**, and `create_session` mutates it from a `to_thread` worker while `get_session` reads from the loop. Dict ops are GIL-atomic so no corruption, but there's no read-modify-write guard and no cap. Contrast the deliberate `threading.Lock` in `EventManagerRegistry` and `JUnitHandlerRegistry`, both of which are correct. The *side effects* of `Session.__init__` on a worker thread (global logging reconfig, driver start, `execution_output` mkdir) are also unserialized. | `common/session_manager.py:153,164,173`; `common/expose_api.py:513` | MED |

## I. Security — relevant now, blocking before multi-tenancy

| ID | Finding | Location |
|----|---------|----------|
| I1 | **No auth, and `session_id` is an unscoped bearer capability.** Every endpoint takes it from the path and does a bare dict lookup with no caller identity and no ownership check. Anyone who obtains a UUID — from a log line, browser history, an error message, a screenshot, an SSE URL — gets **full control of that device**: tap, type, screenshot, page source, `DELETE .../stop`. Statelessness makes this *worse*: an external registry turns the id into a portable cross-node capability. | `common/expose_api.py:887,961,994,1091,1109` |
| I2 | `allow_origins=["*"]` **with** `allow_credentials=True` — starlette then echoes the request `Origin`, effectively permitting **any website** to make credentialed cross-origin calls. Default bind is `127.0.0.1:8000`, so any page the operator visits can enumerate and drive local sessions. | `common/expose_api.py:105-111` |
| I3 | `get_driver_session_id` hands out the underlying Appium session id. Combined with `attach_to_session`, a caller who learns it plus the Appium URL can attach **directly to the device, bypassing optics entirely** — including after the optics session is terminated, if the Appium session outlives it. A capability-escalation primitive, exposed unauthenticated. | `common/expose_api.py:1030-1036`; `api/app_management.py:57-59`; `engines/drivers/appium.py:391` |
| I4 | Session capabilities logged verbatim: `internal_logger.info("Created session %s with config: %s", session_id, config.model_dump())`. Cloud device-farm caps routinely carry access keys / `browserstack.user` / tokens. `log_level` is forced to DEBUG and root is forced to DEBUG, and per C9 `SensitiveDataFormatter` doesn't cover the console handler — which is where containers ship logs. | `common/expose_api.py:498,523-527`; `helper/serve.py:49` |
| I5 | `project_path` is caller-controlled and read from disk — it feeds `discover_templates` and `execution_output_path`, which then `os.makedirs`. An unauthenticated caller chooses a server-side path to enumerate images in and create directories under. (Template *names* are correctly hardened by `_safe_template_filename`; `project_path` itself is not validated.) | `common/expose_api.py:270,501`; `common/config_handler.py:129-131` |
| I6 | No rate limiting on session creation or keyword execution, so G3 is also a trivial unauthenticated DoS. | absent |

## J. Already correct — do not refactor

- **The async seam pattern**, `common/execution.py:189-190`. Sync engines, one `to_thread`, per-session `keyword_lock`. This is the template.
- **`Appium.attach_to_session`** (`engines/drivers/appium.py:391-468`) — a `SessionAttachmentWebDriver` that intercepts `newSession` and returns a synthetic response carrying the target session id, then forces `driver.session_id`. Reachable from config today via `SESSION_ID_CAP_KEYS` (`:65`) and `_try_attach_or_clear_session_caps` (`:184-210`), with fallback to creating a new session. And `launch_app` only starts a session **if `self.driver is None`**, so serve's auto-launch is harmless on the attach path. ⇒ **the serializable state of an Appium session is exactly `(server_url, remote_session_id, capabilities)`.**
- **Session-scoped registries with real locks**: `EventManagerRegistry` (`common/events.py:174-202`) and `JUnitHandlerRegistry` (`common/Junit_eventhandler.py:14-74`), both keyed by `session_id`. `junit_output_<session_id>.xml` and `detected_errors_<session_id>.json` are properly namespaced.
- **Per-session temp dir**: `tempfile.mkdtemp(prefix="optics_session_")`; `terminate_session` `rmtree`s **only** `session._inline_templates_dir`, never a parent. Per-request dirs in `expose_api` likewise.
- **Per-session, no shared state**: `EventSDK`, `OpticsBuilder` (`_instances` is per-builder), `InstanceFallback`, `StrategyManager` + its factories (`_registry` there is an *instance* attribute despite the name), `KeywordRegistry`, `SessionTemplateResolver`. `test_context.current_test_case` is a `ContextVar` — correctly task-isolated.
- **`SessionManager` and `ConfigHandler` are not singletons.** Engines carry only class-level *constants*; `appium_platforms.PROFILES` is populated at import and read-only.
- **No `os.chdir`, no `os.environ` mutation, no `sys.path` mutation, no `lru_cache`/`cached_property`/`__new__` singletons anywhere.** Only `sys.modules` mutation is D2's pytest cleanup. No signal handlers, no `faulthandler`, no `warnings.filterwarnings`, no cv2/matplotlib global tuning.
- **No global-config write during runs.** `~/.optics/global_config.yaml` is written only by the interactive `optics config` TUI; sessions never read it.
- **`workspace_hashes`** is module-level but correctly keyed by `session_id` and popped on teardown (modulo G1).
- **`screenshot_stream.py`** correctly uses thread-safe `queue.Queue` with its own worker threads. **No `asyncio.Queue` is touched from a non-loop thread**; the one broken bridge is B1, which bypasses the queue entirely.
- **`helper/mcp_server.py`** is a clean async pass-through — it inherits `expose_api`'s problems but adds none.
- Server ports are already configurable (`--host/--port`, MCP default 8090); no hardcoded bind.

## K. The statelessness question, honestly

| Layer | Stateless-able? | Notes |
|---|---|---|
| HTTP request handling | **Yes — already is** | No request context beyond path params. |
| Keyword registry / API instances | **Yes — it's derived, not state** | Pure function of config. Cache per session (also fixes A8). |
| Execution state (`ElementData`, `ModuleData`, `test_cases`, `apis`, templates, recording buffer) | **Yes** | All JSON-serializable. |
| Artifacts | **Yes — already is for serve** | `save_captures=False` + base64 responses. Modulo E8, which violates it in practice. |
| Session registry | **Directory yes, object no** | Can externalize `{id → host, caps, remote driver id, TTL}`. Cannot serialize a live `webdriver.Remote`. |
| Event / SSE stream | **No, not without replacing the transport** | `session.event_queue` is an in-process `asyncio.Queue`; an SSE subscriber must land on the owning process. **This is the sleeper cost of multi-process** — a bigger change than replacing the registry, and easy to under-scope. |
| Driver connection | **Appium: effectively yes. Selenium: ~40-line port. Playwright: no.** | Appium re-attaches from `(url, session_id, caps)`. Selenium's `get_driver_session_id` raises `NotImplementedError` and there's no `attach_to_session` — same `newSession`-interception trick, feasible. **Playwright launches its own in-process browser subprocess driven over a pipe by a global loop thread; `get_driver_session_id` returns the constant `"PLAYWRIGHT_SESSION"`; there is no `connect_over_cdp` anywhere. Hard-pinned to the process for its whole life.** |

**Honest definition to adopt:** "stateless `optics serve`" = (a) no server-local *authoritative* state; (b) any instance can serve any request given the externalized handle; (c) a process restart loses no session. Achievable for Appium + inline artifacts + externalized registry + externalized event bus. **Not achievable for in-process Playwright, nor for locally-attached USB devices — there, host affinity is physical, not architectural, and no amount of Redis fixes it.**

Also note: `asyncio.Lock` is process-local. `session.keyword_lock` is the *only* thing preventing interleaved WebDriver commands, and a WebDriver session is not concurrency-safe — so distributed serialization requires a distributed lease, not just a shared registry.

## L. CLAUDE.md drift to fix (per our own hard rule)

- `common/strategies.py` anchors have shifted: `StrategyManager` **`:553`** (doc says `:526`), `locate` **`:679`** (`:610`), `assert_presence` **`:717`** (`:648`), `LocateResult` **`:538`** (`:511`), `_is_method_implemented` **`:78`** (`:66`).
- Doc claims `junit_output.xml`; the event-handler path actually writes `junit_output_<session_id>.xml` (`Junit_eventhandler.py:56`). Only the pytest runner uses the unscoped name (E5).
- Doc lists `logs.json` as an output artefact. `config.json_path` is set (`session_manager.py:46-49`) but **grep shows no writer** — it is never written.
- `Junit_eventhandler.py:50` reads `getattr(config, 'json_log_path', None)`, a field that **does not exist** on `Config` (it has `json_path`). The custom-path branch is dead code; a user-supplied `json_log_path` would silently do nothing.
- `_ensure_global_config` (`config_handler.py:146`) and `GenericFactory.create_instance` / `clear_instances` are dead code.
